import { PostItem, CommentItem, ChatMessage } from '../types';

// All demo posts/comments have been deleted as requested.
export const initialPosts: PostItem[] = [];

export const initialComments: CommentItem[] = [];

export const initialChatMessages: ChatMessage[] = [];

